pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/pruthvik235/docker-automation.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("my-image:latest")
                }
            }
        }
    }
}
